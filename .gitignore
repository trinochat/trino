# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Rust / Tauri
src-tauri/target
/target

# Android build outputs (sources in gen/android stay committed)
src-tauri/gen/android/.gradle
src-tauri/gen/android/build
src-tauri/gen/android/app/build
src-tauri/gen/android/local.properties
src-tauri/gen/android/key.properties
src-tauri/gen/android/.idea

# Signing key material — the announcement PRIVATE key must live OUTSIDE the repo
# (see docs/announcement-channel.md). The public key is intentionally allowed.
*.asc
!public/updates/public-key.asc
*.key
*.pgp
*.gpg
secring.*

# Agent/dev scratch screenshots and UI dumps
.codex-*
